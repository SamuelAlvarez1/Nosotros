name: Deploy to GitHub Pages

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build project
        run: npm run build
      
      - name: Copy dist to root for GitHub Pages
        run: |
          cp dist/index.html index.html
          rm -rf assets stickers
          cp -r dist/assets .
          cp -r dist/stickers . 2>/dev/null || true
          cp dist/config.json config.json
          cp dist/favicon.svg favicon.svg
          cp dist/icons.svg icons.svg
      
      - name: Commit and push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add -A
          git diff-index --quiet HEAD || (git commit -m "chore: Deploy built assets" && git push)
